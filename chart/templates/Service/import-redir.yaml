# Source: k0s/templates/import-redir.yaml
apiVersion: v1
kind: Service
metadata:
  name: import-redir
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: import-redir
